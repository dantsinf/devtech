package ExColecoes.src;

import java.util.HashMap;

public class ExemploMaps {
    public void demo1(){
       /* HashMap<String, ContaCorrente> tab = new HashMap<>();

        tab.put("Felipe", new ContaCorrente(123456));
        tab.put("Carol", new ContaCorrente(345784));
        tab.put("Eduardo", new ContaCorrente(968457));

        System.out.printf("Dados do Felipe: " + tab.get("Felipe").getNroConta());
        System.out.printf("Dados do Carol: " + tab.get("Carol").getNroConta());
        System.out.printf("Dados do Felipe: " + tab.get("Felipe").getNroConta());

        System.out.printf("Dados do Edi: " + tab.get("Edi").getNroConta());
        */

    }
}
